# Ask AI final hardening R2

Branch: `feature/ask-ai-dashboard-authority-r1`. Base: `b9feeed9a0b4ffd39d6c5fca72c85acc4cbdb9ae`.
Production HOLD. No merge or deployment. No second engine/provider path.

## Corrected defects

`AskAiDock` is now a pathname-gated wrapper. Canonical `/ask` (with or without query parameters) returns null before the stateful global dock mounts. Every other path renders the unchanged inner dock. The dashboard's 104px submit margin and extra 60px mobile right padding are removed; normal 8px compact composer padding remains. No split, sheet, map, HUD, or typography contract changes.

`useAskConversation` tracks committed language and bounded StoryContext identity (title, articleId, countryCode). An identity transition invalidates the request generation, releases the local in-flight lock, and clears loading. A late result/error cannot publish or clear a newer pending request. Returning to the original context cannot revive the earlier generation. The commit check also compares the captured language and bounded identity. No cancellation, refund, provider request, or new transport is generated by a context transition. Previous answers remain display-only; a same-context/same-language follow-up sends only the preceding USER question.

## Verification

- Ask/navigation/SEO tests: 8 suites, 201 tests passed.
- Seven deterministic React lifecycle cases use controlled promises: Rwanda -> Kenya; EN -> PL; combined language/context change; unchanged bounded identity with duplicate-submit lock and follow-up payload; stale completion while a newer request is pending; A -> B -> A; stale rejection. Renderer is a React 18 development-only test dependency.
- TypeScript noEmit passed. Changed-file lint passed without warnings.
- Full suite: 273 suites passed, 6 failed; 6,388 tests passed, 8 failed, 13 skipped. Exact failed test names match the untouched R1 baseline: accountProxy (2), adminProvenance (2), adminOperationalSurface (1), c911RequestEconomy (1), m51PhaseB (1), pwaGenerationAuthority (1). They are not classified as R2 regressions. Logs: `logs/ask-r2-tests.log`, `logs/ask-r2-full-tests.{log,json}`.
- Browser matrix: 4/4 passed, including deferred-response suppression and all four global dock routes. Composer screenshots inspected; duplicate launcher absent and normal full-width compact composer restored. Spatial token gate 42/42 and spatial structure gate 100/100 passed. Production build passed, generating 54 pages. Only the two inherited hook-dependency warnings in MapPageClient and GlobalMapShell remain. Build log: `logs/ask-r2-build.log`.

The browser harness checks EN/PL at 1440×900 and 390×844; dedicated composer and zero launcher on `/ask`; zero calls on opening a URL question; exactly one native analysis request for each explicit question; previous USER question only; Q coverage and bounded handoff to `/search`; global dock open on `/search`, `/map`, `/`, `/humanitarian` without analysis; and a real browser deferred-response/context-removal race.

Evidence directory: `D:\Desktop\GlobalNewsAI\Codex_Output\Y\ask-dashboard-r2-evidence` (browser-results.json and desktop/phone EN/PL screenshots). Requests are intercepted test fixtures, never production/provider validation. Recovered v1.8 authority and hashes remain recorded in `docs/ask-dashboard-authority-r1.md`. Existing Polish global-header overflow and unsupported-data/Watch gaps from R1 remain separate; no new visual system or data is introduced.
