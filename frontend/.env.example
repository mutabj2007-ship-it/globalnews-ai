# ---------------------------------------------------------------------------
# GlobalNews AI - Frontend Environment Variables (example)
# Copy this file to `.env.local` in the same directory and fill in real values.
# ---------------------------------------------------------------------------

# Base URL of the backend API (NestJS)
NEXT_PUBLIC_API_URL=http://localhost:4000
