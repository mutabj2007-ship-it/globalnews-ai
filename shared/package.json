{
  "name": "@globalnews-ai/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types, interfaces, and constants used by both frontend and backend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "rimraf": "^5.0.5"
  }
}
